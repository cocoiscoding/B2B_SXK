NODE_ENV=development
VITE_APP_ENV=dev
VITE_PORT=52400
# 开发环境后端 API 地址
# Phase 4：后端未启，此处值不影响功能（所有 /api/* 由下方 Mock 开关短路）
VITE_API_TARGET=http://localhost:8080
# Phase 4：开启本地 Mock 登录/验证码/菜单短路，避免后端未启时 ECONNREFUSED
# 后端就绪后改为 false 即可恢复真实链路
VITE_APP_USE_MOCK_AUTH=true
